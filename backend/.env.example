# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE BASE DE DATOS
# -----------------------------------------------------------------------------
# [CAMBIO PRODUCCIÓN]: Cambia esta URL por la de tu servidor MySQL de producción.
# Si usas el docker-compose.yml incluido, déjalo como está.
DATABASE_URL="mysql://root:root_pass@db:3306/sincro_db"

# Ejemplo para MySQL externo o en HOST local:
# DATABASE_URL="mysql://USER:PASSWORD@IP_SERVIDOR:3306/sincro_db"

# -----------------------------------------------------------------------------
# [CAMBIO PRODUCCIÓN]: ¡OBLIGATORIO! Cambia estos por secretos largos y complejos.
# Puedes usar: openssl rand -base64 48
JWT_SECRET="your-very-long-random-secret-here-for-access-tokens-1234567890"
JWT_REFRESH_SECRET="another-different-very-long-secret-for-refresh-tokens-0987654321"

# Tiempos de expiración (formatos aceptados por zeit/ms, e.g. 15m, 1h, 7d)
ACCESS_TOKEN_EXPIRY="15m"
REFRESH_TOKEN_EXPIRY="7d"

# -----------------------------------------------------------------------------
# SERVER SETTINGS
# -----------------------------------------------------------------------------
PORT=3001
# [CAMBIO PRODUCCIÓN]: Cambia a "production" cuando despliegues en el servidor real.
NODE_ENV="development" # development, production, test

# [CAMBIO PRODUCCIÓN]: Incluye la IP o el dominio de tu servidor de Intranet.
# Ejemplo: CORS_ORIGIN="http://10.0.0.50:5173,http://mi-intranet.com"
CORS_ORIGIN="http://localhost:5173,http://127.0.0.1:5173"

# -----------------------------------------------------------------------------
# MYSQL (solo para docker-compose.yml)
# -----------------------------------------------------------------------------
# Estas variables se usan en docker-compose.yml para configurar el contenedor MySQL.
# Si ejecutas MySQL por separado, ignóralas.
MYSQL_ROOT_PASSWORD=root_pass
MYSQL_DATABASE=sincro_db
MYSQL_ROOT_HOST=%

# -----------------------------------------------------------------------------
# PUERTOS (solo para docker-compose.yml)
# -----------------------------------------------------------------------------
# Puertos del host para cada servicio. Si no se definen, se usan los defaults.
# HOST_PORT_DB=13306
# HOST_PORT_API=13001
# HOST_PORT_FRONTEND=15173
