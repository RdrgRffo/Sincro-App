/**
 * Alias de compatibilidad: el detalle de ausencias (incl. vacaciones) vive en `AbsenceDetailModal`.
 * Conservado para resolver merges donde la rama remota añadía este nombre de componente.
 */
export { AbsenceDetailModal as VacationDetailModal } from './AbsenceDetailModal';
